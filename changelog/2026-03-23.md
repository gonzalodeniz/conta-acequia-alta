## 05:16:36 UTC - doc-teams

### Resumen
- Actualice `doc-teams/guia-instalacion.md` para convertir el entorno virtual de Python en requisito obligatorio y documentar su creacion, activacion y preparacion antes de instalar dependencias o ejecutar la aplicacion.
- Refleje tambien ese requisito en `doc-teams/manual-tecnico.md` para que quede visible junto con el resto de comandos tecnicos utiles.

### Estado
- Documentacion de instalacion alineada sobre `main` con uso obligatorio de entorno virtual.

## 05:49:47 UTC - developer-teams

### Resumen
- Cree la issue tecnica `#5` para parametrizar el puerto de ejecucion local mediante `.env` y abrir su seguimiento operativo en GitHub.
- Actualice `app.py` para leer `PORT` desde `.env` o desde el entorno de ejecucion con valor por defecto `8000`, sin introducir dependencias externas.
- Ajuste `.env.example` para mostrar `PORT=8000` y anadi pruebas para la carga de configuracion y la prioridad del valor de puerto.

### Estado
- Cambio tecnico preparado sobre la rama `issue-5-port-env` y registrado en `main` como trazabilidad operativa.
